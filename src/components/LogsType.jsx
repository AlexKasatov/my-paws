import styled from 'styled-components';
import { SubHeading } from '../theme/typography.styled';
import { UserLogsWrapper } from './UserLogs';

export const NoItemsStyled = styled.div``;

const LogsType = ({ likes, dislikes, favs }) => {
        <UserLogsWrapper>
                <SubHeading />
        </UserLogsWrapper>;
};

export default LogsType;
